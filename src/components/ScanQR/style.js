import {StyleSheet} from 'react-native'

export default StyleSheet.create({
    buttonTouchable: {
        alignItems: 'center',
        justifyContent: 'center'
    },
    image: {
        width: 70,
        height: 70,
    }
});